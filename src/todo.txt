* fix example task to use current API
* add new subject
* deal with errors-- error boundary? useContext?
* git repo onto github
* is there code duplication in reducer?
* new protocol: validate group names are unique
* get specs for a protocol
* format code with prettier
* show overall means?? (all subjects submitted so far)
* don't jump insertion point to the end when rejecting spaces
